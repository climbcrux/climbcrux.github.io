.container {
    position: relative;
}
.container.invalid > div {
    opacity: 0.3;
}

.container.invalid > div:hover {
    pointer-events: none;
}
.container:not(.invalid) > div:hover {
    cursor: pointer;
}
